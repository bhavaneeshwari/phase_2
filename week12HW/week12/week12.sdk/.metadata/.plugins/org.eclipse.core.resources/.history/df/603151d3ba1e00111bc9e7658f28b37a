/*
 * mapper.c
 *
 *  Created on: Mar 13, 2026
 *      Author: vlsiuser
 */

/* AUTO-GENERATED by mapper_gen.py — DO NOT EDIT MANUALLY */

#include <string.h>
#include "mapper.h"
#include "func_desc.h"
#include "../core_logic/afe_drivers.h"
#include "../axi_regs.h"

#define OPERAND_PTR  ((u8 *)HW_OPERAND_BASE)
#define RESULT_PTR   ((u8 *)HW_RESULT_BASE)

/* ------------------------------------------------------------------ */
/* Argument type enum                                                   */
/* ------------------------------------------------------------------ */

/* ------------------------------------------------------------------ */
/* map_spiRawWrite                                                      */
/* driver : afeSpiRawWrite(u8 afeInst, u16 addr, u8 data)              */
/* OPERAND: [afeInst:u8][addr:u16][data:u8]                            */
/* ------------------------------------------------------------------ */
void map_spiRawWrite(void)
{
    u32 offset = 0;

    u8  afeInst; memcpy(&afeInst, OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr;    memcpy(&addr,    OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);
    u8  data;    memcpy(&data,    OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);

    afeSpiRawWrite(afeInst, addr, data);
}

/* ------------------------------------------------------------------ */
/* map_spiRawRead                                                       */
/* driver : afeSpiRawRead(u8 afeInst, u16 addr, u8 *readVal)           */
/* OPERAND: [afeInst:u8][addr:u16]                                     */
/* RESULT : [readVal:u8]                                               */
/* ------------------------------------------------------------------ */
void map_spiRawRead(void)
{
    u32 offset = 0;

    u8  afeInst; memcpy(&afeInst, OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr;    memcpy(&addr,    OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);

    u8 readVal = 0;
    afeSpiRawRead(afeInst, addr, &readVal);

    memcpy(RESULT_PTR, &readVal, sizeof(u8));
}

/* ------------------------------------------------------------------ */
/* map_spiBurstWrite                                                    */
/* driver : afeSpiBurstWrite(u8 afeInst, u16 addr, u8 *data, u16 size)*/
/* OPERAND: [afeInst:u8][addr:u16][size:u16][data:nB]                 */
/* ------------------------------------------------------------------ */
void map_spiBurstWrite(void)
{
    u32 offset = 0;

    u8  afeInst; memcpy(&afeInst, OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr;    memcpy(&addr,    OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);
    u16 size;    memcpy(&size,    OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);

    if (size > MAX_ARGS) return;

    u8 data[MAX_ARGS] = {0};
    memcpy(data, OPERAND_PTR + offset, size);

    afeSpiBurstWrite(afeInst, addr, data, size);
}

/* ------------------------------------------------------------------ */
/* map_spiBurstRead                                                     */
/* driver : afeSpiBurstRead(u8 afeInst, u16 addr, u16 size, u8 *data) */
/* OPERAND: [afeInst:u8][addr:u16][size:u16]                          */
/* RESULT : [data:nB]                                                  */
/* ------------------------------------------------------------------ */
void map_spiBurstRead(void)
{
    u32 offset = 0;

    u8  afeInst; memcpy(&afeInst, OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr;    memcpy(&addr,    OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);
    u16 size;    memcpy(&size,    OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);

    if (size > MAX_ARGS) return;

    u8 data[MAX_ARGS] = {0};
    afeSpiBurstRead(afeInst, addr, size, data);

    memcpy(RESULT_PTR, data, size);
}

/* ------------------------------------------------------------------ */
/* map_spiRawWriteMulti                                                 */
/* driver : afeSpiRawWriteMulti(u8 sel, u16 addr, u8 data)            */
/* OPERAND: [sel:u8][addr:u16][data:u8]                               */
/* ------------------------------------------------------------------ */
void map_spiRawWriteMulti(void)
{
    u32 offset = 0;

    u8  sel;  memcpy(&sel,  OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr; memcpy(&addr, OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);
    u8  data; memcpy(&data, OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);

    afeSpiRawWriteMulti(sel, addr, data);
}

/* ------------------------------------------------------------------ */
/* map_spiRawReadMulti                                                  */
/* driver : afeSpiRawReadMulti(u8 sel, u16 addr, u8 *readVal)         */
/* OPERAND: [sel:u8][addr:u16]                                         */
/* RESULT : [readVal:NUM_SPI bytes]                                    */
/* ------------------------------------------------------------------ */
void map_spiRawReadMulti(void)
{
    u32 offset = 0;

    u8  sel;  memcpy(&sel,  OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr; memcpy(&addr, OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);

    u8 readVal[NUM_SPI] = {0};
    afeSpiRawReadMulti(sel, addr, readVal);

    memcpy(RESULT_PTR, readVal, NUM_SPI);
}

/* ------------------------------------------------------------------ */
/* map_spiBurstWriteMulti                                               */
/* driver : afeSpiBurstWriteMulti(u8 sel, u16 addr, u8 *data, u16 size)*/
/* OPERAND: [sel:u8][addr:u16][size:u16][data:nB]                     */
/* ------------------------------------------------------------------ */
void map_spiBurstWriteMulti(void)
{
    u32 offset = 0;

    u8  sel;  memcpy(&sel,  OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr; memcpy(&addr, OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);
    u16 size; memcpy(&size, OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);

    if (size > MAX_ARGS) return;

    u8 data[MAX_ARGS] = {0};
    memcpy(data, OPERAND_PTR + offset, size);

    afeSpiBurstWriteMulti(sel, addr, data, size);
}

/* ------------------------------------------------------------------ */
/* map_spiBurstReadMulti                                                */
/* driver : afeSpiBurstReadMulti(u8 sel, u16 addr, u16 size, u8 *data)*/
/* OPERAND: [sel:u8][addr:u16][size:u16]                              */
/* RESULT : [data:nB]                                                  */
/* ------------------------------------------------------------------ */
void map_spiBurstReadMulti(void)
{
    u32 offset = 0;

    u8  sel;  memcpy(&sel,  OPERAND_PTR + offset, sizeof(u8));  offset += sizeof(u8);
    u16 addr; memcpy(&addr, OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);
    u16 size; memcpy(&size, OPERAND_PTR + offset, sizeof(u16)); offset += sizeof(u16);

    if (size > MAX_ARGS) return;

    u8 data[MAX_ARGS] = {0};
    afeSpiBurstReadMulti(sel, addr, size, data);

    memcpy(RESULT_PTR, data, size);
}
